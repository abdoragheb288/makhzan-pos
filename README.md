# 🏪 Makhzan POS - نظام مخزن لإدارة نقاط البيع

> نظام SaaS متعدد المستأجرين (Multi-Tenant) متكامل وقوي لإدارة نقاط البيع، المخزون، والفروع - مصمم خصيصاً للملابس والتجزئة.

![Node.js](https://img.shields.io/badge/Node.js-v18+-green)
![React](https://img.shields.io/badge/React-18-blue)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-Neon-purple)
![License](https://img.shields.io/badge/License-Proprietary-red)

---

## 📋 فهرس المحتويات

- [نظرة عامة](#-نظرة-عامة)
- [هيكل المشروع](#-هيكل-المشروع)
- [التقنيات المستخدمة](#-التقنيات-المستخدمة)
- [المميزات الكاملة](#-المميزات-الكاملة)
- [البنية المعمارية](#-البنية-المعمارية)
- [قاعدة البيانات](#-قاعدة-البيانات)
- [API Endpoints](#-api-endpoints)
- [أدوار المستخدمين والصلاحيات](#-أدوار-المستخدمين-والصلاحيات)
- [التشغيل المحلي](#-التشغيل-المحلي)
- [متغيرات البيئة](#-متغيرات-البيئة)
- [النشر (Deployment)](#-النشر-deployment)
- [ملاحظات للمطورين](#-ملاحظات-للمطورين)

---

## 🎯 نظرة عامة

**Makhzan POS** هو نظام SaaS كامل لإدارة نقاط البيع مع دعم كامل للـ Multi-Tenancy. يتيح للشركات:

- ✅ إنشاء حسابات مستقلة لكل شركة (Tenant)
- ✅ إدارة فروع متعددة
- ✅ نظام نقاط بيع سريع ومتجاوب
- ✅ تتبع المخزون في الوقت الفعلي
- ✅ تقارير وتحليلات شاملة
- ✅ نظام ورديات للكاشير
- ✅ نظام تقسيط للعملاء
- ✅ لوحة تحكم Super Admin لإدارة كل المستأجرين

---

## 📁 هيكل المشروع

```
makhzan-pos/
├── backend/                    # الخادم (Node.js + Express)
│   ├── api/                    # Entry point for Vercel
│   │   └── index.js
│   ├── prisma/
│   │   ├── schema.prisma       # تعريف قاعدة البيانات (605 سطر)
│   │   └── seed.js             # بيانات أولية
│   └── src/
│       ├── app.js              # التطبيق الرئيسي
│       ├── config/
│       │   └── database.js     # اتصال Prisma
│       ├── controllers/        # 26 Controller
│       │   ├── auth.controller.js
│       │   ├── pos.controller.js
│       │   ├── product.controller.js
│       │   ├── inventory.controller.js
│       │   ├── shift.controller.js
│       │   ├── sale.controller.js
│       │   ├── purchase.controller.js
│       │   ├── branch.controller.js
│       │   ├── user.controller.js
│       │   ├── tenant.controller.js
│       │   ├── superadmin.controller.js
│       │   └── ... (15 more)
│       ├── middleware/
│       │   ├── auth.middleware.js
│       │   └── error.middleware.js
│       ├── routes/             # 20+ Route files
│       └── utils/
│           └── helpers.js
│
├── frontend/                   # الواجهة (React + Vite)
│   ├── src/
│   │   ├── components/         # مكونات قابلة لإعادة الاستخدام
│   │   │   ├── Header.jsx
│   │   │   ├── Sidebar.jsx
│   │   │   ├── ProtectedRoute.jsx
│   │   │   └── Layout.jsx
│   │   ├── pages/              # 22 صفحة
│   │   │   ├── Dashboard.jsx
│   │   │   ├── POS.jsx
│   │   │   ├── Products.jsx
│   │   │   ├── Inventory.jsx
│   │   │   ├── Branches.jsx
│   │   │   ├── Users.jsx
│   │   │   ├── Shifts.jsx
│   │   │   ├── Sales.jsx
│   │   │   ├── Purchases.jsx
│   │   │   ├── Transfers.jsx
│   │   │   ├── Reports.jsx
│   │   │   ├── Analytics.jsx
│   │   │   ├── Settings.jsx
│   │   │   ├── Installments.jsx
│   │   │   └── superadmin/     # صفحات Super Admin
│   │   ├── services/
│   │   │   ├── api.js          # Axios configuration
│   │   │   └── index.js        # All API services
│   │   ├── store/
│   │   │   └── index.js        # Zustand auth store
│   │   └── styles/             # CSS files
│   └── vite.config.js
│
└── README.md
```

---

## 🛠 التقنيات المستخدمة

### Backend
| التقنية | الإصدار | الوصف |
|---------|---------|-------|
| **Node.js** | v18+ | Runtime |
| **Express.js** | 4.x | Web Framework |
| **Prisma** | 5.x | ORM لقاعدة البيانات |
| **PostgreSQL** | 15+ | قاعدة البيانات (Neon Cloud) |
| **JWT** | - | المصادقة والتوثيق |
| **bcryptjs** | - | تشفير كلمات المرور |

### Frontend
| التقنية | الإصدار | الوصف |
|---------|---------|-------|
| **React** | 18 | مكتبة الواجهة |
| **Vite** | 5.x | Build Tool |
| **Zustand** | 4.x | إدارة الحالة |
| **React Router** | 6.x | التنقل |
| **Axios** | - | HTTP Client |
| **Lucide React** | - | الأيقونات |
| **React Hot Toast** | - | الإشعارات |
| **Recharts** | - | الرسوم البيانية |

### Deployment
| الخدمة | الاستخدام |
|--------|----------|
| **Vercel** | Frontend + Backend (Serverless) |
| **Neon** | PostgreSQL Cloud Database |
| **GitHub** | Source Control |

---

## ✨ المميزات الكاملة

### 1. 🏢 Multi-Tenancy (تعدد المستأجرين)
- كل شركة لها بياناتها المعزولة بالكامل
- عزل البيانات عبر `tenantId` في كل الجداول
- نظام اشتراكات (شهري/ربع سنوي/سنوي)
- فترة تجريبية مجانية
- لوحة تحكم Super Admin

### 2. 🛒 نقطة البيع (POS)
- واجهة سريعة وسهلة الاستخدام
- بحث بالباركود أو الاسم
- تصفية حسب الفئات
- دعم الخصومات (نسبة أو مبلغ)
- طرق دفع متعددة (نقدي/بطاقة/آجل)
- طباعة الفواتير
- حساب الباقي تلقائياً

### 3. 📦 إدارة المنتجات
- منتجات مع متغيرات (Variants): مقاس، لون
- صور المنتجات
- أكواد SKU و Barcode تلقائية
- تصنيفات هرمية (فئة رئيسية وفرعية)
- سعر التكلفة وسعر البيع

### 4. 🏬 إدارة المخزون
- تتبع الكميات في كل فرع
- تنبيهات المخزون المنخفض
- تعديل المخزون يدوياً
- تقرير المخزون

### 5. 🔄 التحويلات بين الفروع
- إنشاء طلبات تحويل
- تتبع حالة التحويل
- تحديث المخزون تلقائياً

### 6. 🛍 أوامر الشراء
- إنشاء أوامر شراء من الموردين
- استلام جزئي أو كامل
- تحديث المخزون تلقائياً عند الاستلام
- ربط بالموردين

### 7. 💳 نظام التقسيط
- بيع بالتقسيط
- جدول سداد
- تتبع الدفعات
- تقارير المستحقات

### 8. ⏰ إدارة الورديات
- فتح وإغلاق الورديات
- رصيد افتتاحي وختامي
- حساب الفرق تلقائياً
- سجل حركات الإيداع والسحب

### 9. 📊 التقارير والتحليلات
- تقرير المبيعات اليومي/الشهري
- تحليل المنتجات الأكثر مبيعاً
- تقرير الأرباح
- تقرير المصروفات
- رسوم بيانية تفاعلية

### 10. 👥 إدارة المستخدمين
- أدوار متعددة (Admin, Manager, Cashier)
- صلاحيات مخصصة لكل صفحة
- ربط المستخدم بفرع محدد

### 11. 💰 إدارة المصروفات
- تسجيل مصروفات الفروع
- تصنيفات المصروفات
- تقارير المصروفات

### 12. 🏷 نظام الخصومات
- كوبونات خصم
- خصم نسبة أو مبلغ
- تاريخ صلاحية
- حد أقصى للاستخدام

### 13. 🔐 Super Admin Dashboard
- عرض كل الشركات (Tenants)
- إيقاف/تفعيل الحسابات
- إحصائيات شاملة
- إدارة الاشتراكات

---

## 🏗 البنية المعمارية

### Multi-Tenant Architecture
```
┌─────────────────────────────────────────────────────┐
│                   Super Admin                        │
│           (يدير كل الشركات والاشتراكات)              │
└─────────────────────────────────────────────────────┘
                         │
    ┌────────────────────┼────────────────────┐
    │                    │                    │
┌───▼────┐          ┌────▼───┐          ┌────▼───┐
│Tenant 1│          │Tenant 2│          │Tenant 3│
│ شركة أ  │          │ شركة ب  │          │ شركة ج  │
└────┬───┘          └────┬───┘          └────┬───┘
     │                   │                   │
┌────▼────────────┐ ┌────▼────────────┐ ┌────▼────────────┐
│ - Users         │ │ - Users         │ │ - Users         │
│ - Branches      │ │ - Branches      │ │ - Branches      │
│ - Products      │ │ - Products      │ │ - Products      │
│ - Sales         │ │ - Sales         │ │ - Sales         │
│ - Inventory     │ │ - Inventory     │ │ - Inventory     │
└─────────────────┘ └─────────────────┘ └─────────────────┘
```

### Data Flow
```
Frontend (React) 
    ↓ HTTP Request + JWT Token
API Routes (Express)
    ↓ Auth Middleware (verify JWT + extract tenantId)
Controllers
    ↓ Filter by tenantId
Prisma ORM
    ↓ SQL Query with tenant filter
PostgreSQL (Neon)
```

---

## 🗄 قاعدة البيانات

### الجداول الرئيسية (17 جدول)

#### Multi-Tenant
| الجدول | الوصف |
|--------|-------|
| `super_admins` | مدراء النظام العامين |
| `tenants` | الشركات/المستأجرين |
| `subscriptions` | اشتراكات الشركات |

#### المستخدمين والفروع
| الجدول | الوصف |
|--------|-------|
| `users` | المستخدمين (Admin, Manager, Cashier) |
| `branches` | الفروع والمخازن |

#### المنتجات
| الجدول | الوصف |
|--------|-------|
| `categories` | تصنيفات المنتجات |
| `products` | المنتجات الأساسية |
| `product_variants` | متغيرات المنتج (مقاس/لون) |
| `inventory` | المخزون لكل فرع |

#### المبيعات
| الجدول | الوصف |
|--------|-------|
| `sales` | فواتير البيع |
| `sale_items` | عناصر الفاتورة |
| `shifts` | الورديات |
| `cash_transactions` | حركات الصندوق |

#### المشتريات
| الجدول | الوصف |
|--------|-------|
| `suppliers` | الموردين |
| `purchase_orders` | أوامر الشراء |
| `purchase_items` | عناصر أمر الشراء |

#### التحويلات
| الجدول | الوصف |
|--------|-------|
| `stock_transfers` | تحويلات المخزون |
| `stock_transfer_items` | عناصر التحويل |

#### أخرى
| الجدول | الوصف |
|--------|-------|
| `expenses` | المصروفات |
| `discounts` | الخصومات والكوبونات |
| `installments` | خطط التقسيط |
| `installment_payments` | دفعات التقسيط |
| `audit_logs` | سجل العمليات |

### العلاقات الرئيسية
```
Tenant (1) ──────► Users (*)
Tenant (1) ──────► Branches (*)
Tenant (1) ──────► Products (*)
Tenant (1) ──────► Categories (*)
Tenant (1) ──────► Suppliers (*)

Branch (1) ──────► Inventory (*)
Branch (1) ──────► Sales (*)
Branch (1) ──────► Shifts (*)

Product (1) ─────► Variants (*)
Variant (1) ─────► Inventory (*)
Variant (1) ─────► SaleItems (*)
```

---

## 🔌 API Endpoints

### Authentication
```
POST   /api/auth/login              # تسجيل الدخول
POST   /api/auth/register-tenant    # تسجيل شركة جديدة
GET    /api/auth/profile            # الملف الشخصي
PUT    /api/auth/profile            # تحديث الملف الشخصي
```

### POS
```
GET    /api/pos/products            # منتجات نقطة البيع
POST   /api/pos/sale                # إنشاء فاتورة
GET    /api/pos/categories          # الفئات
GET    /api/pos/barcode/:code       # بحث بالباركود
```

### Products
```
GET    /api/products                # كل المنتجات
GET    /api/products/:id            # منتج محدد
POST   /api/products                # إضافة منتج
PUT    /api/products/:id            # تعديل منتج
DELETE /api/products/:id            # حذف منتج
POST   /api/products/:id/variants   # إضافة متغير
```

### Branches
```
GET    /api/branches                # كل الفروع
GET    /api/branches/:id            # فرع محدد
POST   /api/branches                # إضافة فرع
PUT    /api/branches/:id            # تعديل فرع
DELETE /api/branches/:id            # حذف فرع
```

### Inventory
```
GET    /api/inventory               # كل المخزون
GET    /api/inventory/branch/:id    # مخزون فرع
PUT    /api/inventory               # تحديث المخزون
GET    /api/inventory/low-stock     # المخزون المنخفض
```

### Sales
```
GET    /api/sales                   # كل المبيعات
GET    /api/sales/:id               # فاتورة محددة
GET    /api/sales/invoice/:number   # بحث برقم الفاتورة
POST   /api/sales/:id/refund        # إرجاع
```

### Shifts
```
GET    /api/shifts                  # كل الورديات
GET    /api/shifts/current          # الوردية الحالية
POST   /api/shifts/open             # فتح وردية
POST   /api/shifts/:id/close        # إغلاق وردية
POST   /api/shifts/:id/transaction  # حركة صندوق
```

### Users
```
GET    /api/users                   # كل المستخدمين
GET    /api/users/:id               # مستخدم محدد
POST   /api/users                   # إضافة مستخدم
PUT    /api/users/:id               # تعديل مستخدم
DELETE /api/users/:id               # حذف مستخدم
```

### Purchases
```
GET    /api/purchases               # كل أوامر الشراء
GET    /api/purchases/:id           # أمر شراء محدد
POST   /api/purchases               # إنشاء أمر شراء
PUT    /api/purchases/:id/receive   # استلام البضاعة
```

### Transfers
```
GET    /api/transfers               # كل التحويلات
POST   /api/transfers               # إنشاء تحويل
```

### Reports
```
GET    /api/reports/sales           # تقرير المبيعات
GET    /api/reports/products        # تقرير المنتجات
GET    /api/reports/expenses        # تقرير المصروفات
```

### Super Admin
```
POST   /api/superadmin/login        # دخول Super Admin
GET    /api/superadmin/tenants      # كل الشركات
GET    /api/superadmin/stats        # الإحصائيات
PUT    /api/superadmin/tenants/:id  # تعديل شركة
```

---

## 👤 أدوار المستخدمين والصلاحيات

### الأدوار
| الدور | الوصف | الصلاحيات الافتراضية |
|-------|-------|---------------------|
| **ADMIN** | مدير الشركة | كل الصلاحيات |
| **MANAGER** | مشرف | معظم الصلاحيات ما عدا الإعدادات الحساسة |
| **CASHIER** | كاشير | نقطة البيع والورديات فقط |

### الصلاحيات المخصصة
```javascript
const AVAILABLE_PERMISSIONS = [
    'pos',        // نقاط البيع
    'products',   // المنتجات
    'inventory',  // المخزون
    'transfers',  // التحويلات
    'users',      // المستخدمين
    'settings',   // الإعدادات
    'reports',    // التقارير
];
```

---

## 🚀 التشغيل المحلي

### المتطلبات
- Node.js v18+
- npm أو yarn
- PostgreSQL (أو حساب Neon مجاني)

### 1. Backend
```bash
cd backend
npm install

# إنشاء ملف .env
cp .env.example .env
# عدّل DATABASE_URL و JWT_SECRET

# إعداد قاعدة البيانات
npm run db:push     # إنشاء الجداول
npm run db:seed     # بيانات أولية

# التشغيل
npm run dev
# يعمل على http://localhost:5000
```

### 2. Frontend
```bash
cd frontend
npm install

# إنشاء ملف .env
echo "VITE_API_URL=http://localhost:5000/api" > .env

# التشغيل
npm run dev
# يعمل على http://localhost:5173
```

### 3. بيانات الدخول (بعد Seed)
```
Admin:
  Email: admin@makhzan.com
  Password: admin123

Cashier:
  Email: cashier@makhzan.com
  Password: cashier123

Super Admin:
  URL: /superadmin/login
  Email: super@makhzan.com
  Password: super123
```

---

## ⚙ متغيرات البيئة

### Backend (.env)
```env
# قاعدة البيانات
DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"

# JWT
JWT_SECRET="your-super-secret-key-min-32-chars"

# بيئة التشغيل
NODE_ENV="development"  # أو production
PORT=5000
```

### Frontend (.env)
```env
VITE_API_URL="http://localhost:5000/api"
# أو للـ Production:
# VITE_API_URL="https://your-backend.vercel.app/api"
```

---

## 🌐 النشر (Deployment)

### 1. قاعدة البيانات - Neon
1. إنشاء حساب على [neon.tech](https://neon.tech)
2. إنشاء Project جديد
3. نسخ Connection String
4. تشغيل الـ migrations محلياً:
   ```bash
   cd backend
   DATABASE_URL="your-neon-url" npm run db:push
   DATABASE_URL="your-neon-url" npm run db:seed
   ```

### 2. Backend + Frontend - Vercel
1. رفع الكود على GitHub
2. إنشاء مشروع على [Vercel](https://vercel.com)
3. ربط الـ Repository
4. إعداد Environment Variables:
   ```
   DATABASE_URL = (من Neon)
   JWT_SECRET = (كلمة سر قوية)
   ```
5. Deploy

---

## 📝 ملاحظات للمطورين

### TenantId Isolation
كل Controllers تستخدم `req.user.tenantId` للفلترة:
```javascript
// مثال من product.controller.js
const where = {
    tenantId: req.user.tenantId
};
```

### Controllers تحتاج مراجعة
- `discount.controller.js` - نموذج Discount ليس لديه `tenantId` (يحتاج migration)

### Known Issues
- مشكلة حفظ الـ `branchId` في تحديث المستخدم - تحتاج تحقق إضافي

### المجلدات المهمة
```
backend/src/controllers/  # منطق الأعمال
backend/src/middleware/   # Auth و Error handling
backend/prisma/          # Schema و Migrations
frontend/src/pages/      # واجهات المستخدم
frontend/src/services/   # API calls
```

---

## 📄 الرخصة

هذا المشروع ملكية خاصة. جميع الحقوق محفوظة.

---

## 📞 التواصل

للاستفسارات التقنية أو طلب المساعدة في التطوير، تواصل مع مالك المشروع.
